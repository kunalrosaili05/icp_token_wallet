# Various IDEs and Editors
.vscode/
.idea/
**/*~

# Mac OSX temporary files
.DS_Store
**/.DS_Store

# dfx temporary files (commented out to track them)
#.dfx/

# generated files (commented out to track them)
#**/declarations/

# rust (optional: you can track `target/` if needed, but it’s usually better not to)
#target/

# frontend code
node_modules/
dist/
.svelte-kit/

# environment variables (optional: only track `.env` if you’re comfortable with that)
.env
